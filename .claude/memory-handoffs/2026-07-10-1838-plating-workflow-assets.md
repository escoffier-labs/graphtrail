# Memory Handoff

## Type
workflow

## Title
GraphTrail workflow graphics are generated by Plating

## Summary
GraphTrail's 2 README workflow graphics come from Plating JSON sources. Future content changes should edit the JSON and regenerate the SVG, not hand-edit SVG coordinates.

## Durable facts
- Quickstart source: `docs/assets/workflows/quickstart.json`.
- Relationship source: `docs/assets/workflows/relationships.json`.
- Generated outputs remain `docs/assets/graphtrail-context.svg` and `docs/assets/graph-relationships.svg` so README links stay stable.
- Regenerate with `plating workflow docs/assets/workflows/quickstart.json --out docs/assets/graphtrail-context.svg` and the equivalent relationship command.
- Workflow specs do not set project colors. Plating applies the canonical dark-ledger palette from `escoffier-fleet-kit/DESIGN.md`.

## Evidence
- files changed: `README.md`, `docs/assets/workflows/*.json`, `docs/assets/*.svg`
- verification: both SVGs parsed as XML and reproduced byte-for-byte; `cargo test --all-features` passed through Brigade

## Recommended memory action
no-card

## Target document
rules/visual-assets.md

## Suggested document content
### GraphTrail workflow images

Edit the JSON sources under `docs/assets/workflows/`, then regenerate the checked-in SVGs with `plating workflow`. Do not hand-position elements in `graphtrail-context.svg` or `graph-relationships.svg`, and do not add project-specific colors to the specs. Plating owns the canonical Escoffier Labs dark-ledger palette. Commit each JSON source with its generated SVG.
